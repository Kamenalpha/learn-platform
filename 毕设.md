
## 三、核心功能模块设计
系统分为**学生用户端**和**管理员后台**两大模块。

### 1. 学生用户端（核心功能）
- **智能问答模块**
  - 自然语言提问，支持上下文多轮对话
  - 回答附带原文引用来源（页码/文档名），可溯源
  - 支持追问、知识点拓展解释
- **知识库浏览模块**
  - 按课程分类浏览知识库（如数图、图形学、视频处理等）
  - 文档预览、知识点目录树展示
- **学习辅助模块**
  - 考点自动生成：输入章节自动输出知识点梳理与练习题
  - 知识图谱可视化：展示知识点关联关系
  - 学习历史记录：保存问答记录，支持收藏

### 2. 管理员后台（支撑功能）
- **文档管理模块**
  - 支持PDF、Word、PPT、TXT格式文档批量上传
  - 文档解析状态监控、手动调整分块
  - 课程分类管理、标签体系维护
- **知识库管理**
  - 向量库增量更新、删除重建
  - 检索参数配置（相似度阈值、召回数量）
- **系统管理**
  - 用户管理、角色权限分配
  - 问答日志统计、系统运行监控

## 四、详细技术栈选型
### 主推方案：Java技术栈（企业级，求职加分）

| 层级 | 技术选型 | 选型说明 |
|------|----------|----------|
| 后端框架 | Spring Boot 3.2.x | 主流企业级框架，生态完善 |
| AI编排 | Spring AI | Spring官方AI组件，原生集成大模型与向量数据库 |
| 数据库 | MySQL 8.0 | 存储用户、文档、日志等业务数据 |
| 向量数据库 | Milvus / Chroma | Milvus适合生产级，Chroma轻量易部署 |
| 缓存 | Redis 7.x | 缓存对话上下文、热点问答结果 |
| 文档解析 | Apache POI + PDFBox | 解析Word、PPT、PDF格式文件 |
| 大模型 | 通义千问 / DeepSeek API | 调用云端API，无需本地算力 |
| 嵌入模型 | bge-small-zh | 中文效果好，可本地轻量运行 |
| 前端 | Vue3 + Element Plus | 快速搭建管理后台与交互界面 |
| 可视化 | ECharts | 知识图谱、统计图表展示 |

### 备选方案：Python技术栈
- 后端：FastAPI + LangChain
- 向量库：Chroma / FAISS
- 文档解析：LangChain文档加载器 + PyPDF2
- 前端：Vue3 或 Streamlit快速原型

## 五、RAG核心业务流程
1. **文档摄入与解析**
   上传课件文档，通过解析工具提取纯文本内容，保留标题层级、段落结构信息。

2. **文本分块处理**
   采用**递归字符分割法**，按语义块拆分文本（如每块500字符，重叠50字符），避免知识点被切断。

3. **向量嵌入生成**
   调用中文嵌入模型，将每个文本块转换为多维向量，同时存储原文内容与元数据（文档名、页码、章节）。

4. **向量入库存储**
   将向量与对应元数据写入向量数据库，建立索引以支持相似度检索。

5. **用户提问检索**
   用户提问时，先将问题转为向量，在向量库中做相似度检索，召回Top-K个最相关的知识块。

6. **召回结果重排**
   可选加入重排序模型（BGE-Reranker），对召回结果二次排序，提升精准度。

7. **大模型生成回答**
   将检索到的知识块 + 用户问题 + 提示词模板拼接，送入大模型，生成基于专业知识库的精准回答，并附上引用来源。

## 六、核心数据库表设计
### 1. 用户表 sys_user
| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| user_id | BIGINT | 主键，用户ID |
| username | VARCHAR(50) | 用户名 |
| password | VARCHAR(100) | 加密密码 |
| role | TINYINT | 角色：0学生 1管理员 |
| create_time | DATETIME | 创建时间 |

### 2. 文档表 course_document
| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| doc_id | BIGINT | 主键，文档ID |
| course_name | VARCHAR(100) | 所属课程名称 |
| doc_title | VARCHAR(200) | 文档标题 |
| file_type | VARCHAR(20) | 文件类型：pdf/word/ppt |
| file_url | VARCHAR(255) | 文件存储路径 |
| parse_status | TINYINT | 解析状态：0未解析 1已解析 2失败 |
| create_time | DATETIME | 上传时间 |

### 3. 问答记录表 qa_record
| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| record_id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| question | TEXT | 用户问题 |
| answer | TEXT | 系统回答 |
| reference | TEXT | 引用来源 |
| session_id | VARCHAR(64) | 对话会话ID |
| create_time | DATETIME | 提问时间 |

## 七、毕业论文完整大纲
1. 绪论
   - 研究背景与意义
   - 国内外研究现状（大模型、RAG、教育领域应用）
   - 主要研究内容与论文结构
2. 相关技术基础
   - 检索增强生成（RAG）技术原理
   - 向量数据库与嵌入模型
   - Spring Boot与Spring AI框架
   - 前端相关技术
3. 系统需求分析
   - 可行性分析
   - 功能性需求（用例图）
   - 非功能性需求
4. 系统总体设计
   - 系统架构设计
   - 功能模块划分
   - 数据库设计
   - RAG核心流程设计
5. 系统详细实现
   - 开发环境搭建
   - 文档解析与分块模块实现
   - 向量检索模块实现
   - 智能问答模块实现
   - 前端界面实现
6. 系统测试
   - 测试环境
   - 功能测试
   - 性能测试
   - 问答效果评估
7. 总结与展望
   - 工作总结
   - 不足与展望
参考文献、致谢

## 八、开发时间规划（按12周毕设周期）
1. **第1-2周**：需求分析、文献查阅、开题报告撰写
2. **第3周**：环境搭建、数据库设计、项目骨架搭建
3. **第4-5周**：后端基础模块开发（用户管理、文档上传管理）
4. **第6-7周**：RAG核心模块开发（文档解析、向量入库、检索问答）
5. **第8周**：前端页面开发与前后端联调
6. **第9周**：辅助功能开发（考点生成、知识图谱、统计）
7. **第10周**：系统测试、bug修复、效果优化
8. **第11-12周**：论文撰写、修改、答辩准备

## 九、毕设加分亮点与答辩技巧
### 1. 加分亮点
- **专业垂直性**：针对数媒专业课程优化，区别于通用问答系统，可做领域prompt工程优化
- **工程完整性**：完整的前后端分离架构、权限管理、日志系统，符合企业开发规范
- **可视化展示**：知识图谱、问答统计、检索过程可视化，答辩演示效果好
- **可扩展性**：预留多模态接口，可扩展图片、视频知识点检索

### 2. 答辩演示建议
- 提前导入3-5门核心课程课件（数字图像处理、计算机图形学等）
- 准备专业问题演示，对比通用大模型与本系统的回答差异，体现RAG的精准性
- 展示后台文档管理、向量库管理流程，体现系统工程完整性

## 十、避坑提醒
1. **不要尝试微调大模型**：本科毕设无需做模型训练，重点放在RAG工程实现与系统集成上
2. **优先用API**：嵌入模型可本地轻量运行，生成大模型直接调用云端API，节省算力与时间
3. **数据就近取材**：用自己的专业课件、教材PDF即可，不用额外找大量数据
4. **突出系统设计**：答辩重点讲架构设计、RAG流程优化、交互设计，不要纠结算法原理细节
