# 项目约束(开发与协作规范)

> 本文件为项目的**强制工作流约束**,所有修改与提交须遵守。Git 分支管理规则参考团队版《Git 分支管理项目规范规则》按本项目(单人毕设、本地仓库无远程)裁剪,原文存于个人知识库 `raw/`;若后续转为多人协作,按团队版升级(develop 集成分支 + MR 互审 + 线上回滚双人确认)。

## 1. 修改与提交流程(必须遵守)

1. **修改前检查**:每次进行修改前,先运行 `git status` 检查是否存在未提交的修改。
   - 若存在未提交的修改,**先提交这些修改**(确保能回退到所需版本),再进行新的修改。
2. **修改后编译测试**:修改完成后,执行编译/构建验证:
   - 后端:`mvn -o compile`(使用 JDK 17,本机路径 `E:\JDK\jdk-17.0.12`)
   - 前端:`npm run build`
3. **编译通过后才提交**:仅当编译/构建**通过**后,才执行 `git commit`。编译失败时须修正后再提交。
4. **大改动走分支**:大规模重构/结构性修改、新功能开发必须走第 2 节的临时分支流程,不得直接在 `main` 上开发。

## 2. Git 分支管理

### 分支模型

- **常驻分支 `main`**:稳定可交付基准(可演示、可写入论文的版本)。仅合入自测通过的代码;日常小改动(bug 修复、小功能、文档)可直接在 `main` 上按第 1 节流程进行。
- **临时分支**(一律从最新 `main` 切出,任务完成后合并、删除):

| 分支 | 适用场景 | 命名格式 | 示例 |
|------|----------|----------|------|
| feature | 新功能开发、功能优化 | `feature/主题` | feature/user-login |
| bugfix | 功能缺陷修复 | `bugfix/主题` | bugfix/quote-parse |
| hotfix | 已交付版本(打过 tag)的紧急修复 | `hotfix/主题` | hotfix/deploy-crash |
| refactor | 大规模重构、跨模块/跨层批量改动、目录或包结构调整、框架/依赖迁移 | `refactor/主题` | refactor/layer-split |
| release | 版本交付前的回归固化 | `release/v版本号` | release/v1.0.0 |

- 分支命名一律**英文小写、横线分隔,禁止中文与特殊符号**;有需求/问题编号时置于主题前(如 `feature/101-user-login`)。

### 合并流程(每条临时分支)

1. 开发期间在分支上按第 1 节流程提交(修改前检查、编译验证、通过后提交)。
2. 合并前同步 `main` 最新代码并解决冲突——谁开发谁解决,禁止盲目保留单侧代码后不重新自测。
3. **全量自测通过后才合并**:后端 `mvn -o test` + 前端 `npm run build`。
4. 合回 `main` 后**及时删除临时分支**(3 个工作日内),避免分支堆积。
5. 版本里程碑(`release` 合并或重要交付)后打标签:`v主.次.补丁`(重大架构重构升主版本,批量新功能升次版本,修复与小优化升补丁),标签备注含更新内容与日期。

### 禁止行为

- **禁止大规模重构直接在 `main` 上开发**(必须走 `refactor` 分支)。
- **禁止合入未自测或测试失败的代码**到 `main`。
- **禁止 `git push -f` 强推、`reset`/`rebase` 已合并入 `main` 的提交**(需回退时先确认可恢复性再操作)。
- **禁止临时分支长期保留**、跨分支开发(新任务从基准分支重新切出)。
- **禁止模糊、无意义的提交日志**(衔接第 3 节)。

## 3. 提交信息规范(Conventional Commits)

格式:`<type>(<scope>): <description>`

- **`description` 一律使用中文简述本次修改概要**(不用英文),让 `git log` 不经翻译即可读。
- 一次提交只做一件事,不把无关改动混在同一个提交里。

| type | 含义 |
|------|------|
| feat | 新功能 |
| fix | 修复 Bug |
| refactor | 重构(不改变功能) |
| style | 样式修改 |
| docs | 文档更新 |
| test | 测试相关 |
| chore | 构建/工具变更 |

示例:`feat(platform): 重构为通用多学科智能学习平台`

## 4. 其它约定

- **数据库**:新库/建表脚本放在 `sql/`(如 `sql/init_learning.sql`),统一 `utf8mb4`。
- **技术栈**:后端 Spring Boot + Spring AI;前端 Vue3 + Element Plus;AI 走云端 API,不做模型微调。
- **目录**:前端页面在 `frontend/src/views/`,接口封装在 `frontend/src/api/index.js`,后端按 controller/service/mapper/entity 分层。
- **文档命名**:项目文档使用中文文件名;`README.md` 与 `AGENTS.md` 例外——二者是平台/工具链按固定名识别的功能性文件,更名会破坏 GitHub 首页渲染与 agent 工作区规则加载。
