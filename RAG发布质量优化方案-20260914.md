# RAG 项目发布质量优化方案

- **编制日期**：2026-09-14
- **依据文档**：《RAG发布质量评估报告-20260914.md》（下称"评估报告"）
- **决策输入**（项目负责人已裁定）：
  - 批次范围：**仅阻断级 P0** + D-2 补社区审核流 + D-5 补审计与越权测试
  - D-3（未登录/登录读公共教材范围）、D-4（?token= URL 保留）、D-6（AI 评分失败降 0 分）、D-9（配额 fail-open）：**确认为设计意图，不改代码，仅文档标注**
  - D-1（两处 detail 越权）：随 P0-2 按bug 修复（评估报告建议 + 阻断标准"安全底线"）

---

## 一、评估报告解读摘要（依据提取）

| 报告条目 | 严重程度 | 影响范围 | 本方案处置 |
|---|---|---|---|
| P0-1 JWT 密钥硬编码无环境变量入口 | 阻断·安全 | 全部管理端接口可被伪造 token 访问 | 本次修复 |
| P0-2 ProjectService/StudyPlanService detail 越权（IDOR） | 阻断·安全 | 任意登录用户可读他人项目辅导/学习计划 | 本次修复 |
| P0-3 README 手动部署缺 upgrade_news_fulltext.sql → 资讯 INSERT 报错 | 阻断·可运行性 | 按文档复现必坏 | 本次修复 |
| P0-4 README 缺 upgrade_fulltext_index.sql → 混合检索静默降级 | 阻断·可运行性/文档失实 | 论文核心链路承诺不符 | 本次修复 |
| P0-5 compose 未传 QUOTA_*/NEWS_* 环境变量 | 阻断·可运行性/文档失实 | Docker 部署下配额与资讯配置静默失效 | 本次修复 |
| P0-6 compose 链路未实测 + chroma 无 healthcheck | 阻断·未实测项 | 部署可信度 | 本次实测并修复 |
| D-2 社区帖发布即 auditStatus=1 | 高风险·口径不一致 | 审核防线对社区失效 | 用户裁定：**补审核流**（超出 P0 的范围增量） |
| D-5 审计模块零测试 | 测试空白 | "测试与回归"阻断标准不完整 | 用户裁定：**补审计+越权用例** |
| H1-H10、中风险 1-17 | 高/中风险 | 不在本次范围 | 后续批次 |
| D-7（.env 真实 Key 轮换） | 提醒类 | 运维操作，无代码改动 | 列为发布前人工动作 |

---

## 二、优化范围界定

### 2.1 本次优化（6 个工作包）

| 工作包 | 内容 | 范围依据 |
|---|---|---|
| WP1 | JWT 密钥环境变量化（后端 yml + compose + .env.example + README） | 评估报告 P0-1 |
| WP2 | 两处 detail IDOR 修复 + 越权回归测试 | 评估报告 P0-2、D-1、D-5 |
| WP3 | SQL 建库脚本收敛（content 列与全文索引并入 init_learning.sql + upgrade 脚本加守卫）+ README 手动部署步骤修正 | 评估报告 P0-3、P0-4 |
| WP4 | compose backend environment 补齐 QUOTA_*/NEWS_*/JWT_SECRET + .env.example 同步 | 评估报告 P0-5 |
| WP5 | 社区帖审核流（发帖待审 + 管理端审核接口与页面 + feed 口径调整） | 用户裁定 D-2 |
| WP6 | docker compose 链路实测 + chroma healthcheck + README 七步验收 | 评估报告 P0-6 |

### 2.2 后续优化（明确不做，依据）

- **H1-H10 全部高风险**（管理员 URI 前缀匹配、事务内调 LLM、错误信息泄露、悬浮球事件名、多表删事务、弱口令开关、Redis 单点、死代码视图、端口、baseURL）：用户裁定"仅阻断级 P0"。其中 H9（README 端口 5173/5174）为文档一行修复且直接影响答辩演示，**建议随 WP6 顺手处理**（见 6.7），是否纳入请确认。
- **中风险 1-17**：同上裁定。
- **D-3/D-4/D-6/D-9**：确认为设计意图，本次仅在 README 增加标注（并入 WP6 文档动作）。
- **D-7**（Key 轮换）、**D-8**（HTTP 恒 200 口径）：前者为运维人工动作无代码，后者未裁定保持现状。

---

## 三、优化对象清单

| # | 优化对象 | 类型 | 依据 | 关联对象 |
|---|---|---|---|---|
| O1 | `backend/src/main/resources/application.yml:73`（jwt-secret 行） | 配置 | P0-1 | O4（compose 注入）、README |
| O2 | `backend/.../service/ProjectService.java:74-80`（detail 方法） | 后端代码 | P0-2 | O3、测试 T1 |
| O3 | `backend/.../service/StudyPlanService.java:31-39`（detail 方法） | 后端代码 | P0-2 | O2、测试 T1 |
| O4 | `docker-compose.yml:68-84`（backend environment 块）、`:46-55`（chroma 服务块）、`:62-67`（depends_on） | 部署配置 | P0-5、P0-6 | O1、.env.example |
| O5 | `.env.example` | 部署配置 | P0-1、P0-5 | O1、O4 |
| O6 | `sql/init_learning.sql`（knowledge_news 建表 + doc_chunk 建表） | SQL | P0-3、P0-4 | O7 |
| O7 | `sql/upgrade_fulltext_index.sql`（加 information_schema 守卫） | SQL | P0-3/P0-4 冲突应对（见 5.3） | O6 |
| O8 | `backend/.../service/CommunityService.java:49-59`（createPost）、`:33-35`（feed） | 后端代码 | D-2 | O9、O10、T3 |
| O9 | `backend/.../controller/AdminAuditController.java`（pending + decidePost） | 后端代码 | D-2 | O8、O10、T2 |
| O10 | `backend/.../mapper/PostMapper.java`（listFeed 增量）、`frontend/src/views/admin/Review.vue`、`frontend/src/api/index.js` | 前后端代码 | D-2 | O8、O9 |
| O11 | `backend/src/test/`（新增 ProjectServiceTest / StudyPlanServiceTest / 审计相关测试） | 测试 | D-5、P0-2 | O2、O3、O9 |
| O12 | `README.md`（部署步骤、环境变量表、设计与安全取舍标注） | 文档 | P0-3/4/5、D-3/4/6/9、H9(待确认) | 全部 |

---

## 四、优化目标（可衡量、可验证）

| 目标 | 对应问题 | 验证方式 | 达成判定 |
|---|---|---|---|
| G1：JWT 密钥可通过环境变量覆盖，仓库默认值不再唯一生效 | P0-1 | 设 `JWT_SECRET=自定义值` 启动后端，旧 token 401、新签 token 有效；`.env.example`/compose/README 三处同步出现该变量 | 环境变量注入生效 + 三处文档同步 |
| G2：非归属者访问他人项目/计划详情返回 403 | P0-2 | student A 的 token 请求 B 的 projectId/planId → 403；归属者 → 200；新增单测覆盖两服务 | 单测通过 + 接口手测 |
| G3：新库仅执行 `init_learning.sql` 即具备 content 列与 ngram 全文索引，资讯抓取与混合检索双路均正常 | P0-3、P0-4 | 空库执行 init_learning.sql 后：①`SHOW COLUMNS` 见 content 列、`SHOW INDEX` 见 ft_doc_chunk_content；②资讯手动抓取接口成功；③管理端检索测试页显示关键词路召回 | 三项全部成立 |
| G4：Docker 部署下 QUOTA_*/NEWS_* 环境变量实际进入容器 | P0-5 | `docker compose exec backend env \| grep -E "QUOTA\|NEWS"` 显示值；改 QUOTA_CHAT_LIMIT 为极小值后问答被限 | env 可见 + 行为生效 |
| G5：社区帖发布后进入待审核，管理员审核通过后出现在 feed | D-2 | 发帖 → feed 不可见（他人视角）→ 管理端审核通过 → feed 可见；驳回 → 持续不可见；发帖人视角 feed 始终可见自己的帖子 | 全链路手测 + 单测 |
| G6：`docker compose up -d --build` 一键成功，README 七步验收全通过 | P0-6 | 完整实测 + 验收记录（数据卷为空首建 + 已有数据重启两种场景） | 验收记录留档 |
| G7：后端测试全过且新增用例 ≥ 6 条（越权 4 + 审核 2+） | D-5 | `mvn -o test` 全绿，用例数从 34 增至 ≥ 40 | 测试报告 |

---

## 五、项目结构冲突分析

### 5.1 结构梳理（优化对象所处位置）

- 后端按 controller → service → mapper 分层，鉴权统一走 `AuthInterceptor` + `UserContext`（thread-local）；`/api/admin/**` 由拦截器强制 role=1。
- SQL 脚本双消费路径：**Docker 路径**（`./sql` 挂载 `/docker-entrypoint-initdb.d`，按文件名字母序全量执行）与**手动路径**（README 指定脚本逐个执行）。两条路径对同一批脚本的消费方式不同，是 WP3 冲突分析的根源。
- 社区模块数据流：`CommunityService.createPost` 写 post 表（audit_status）→ `PostMapper.listFeed` SQL 过滤 `audit_status=1` → 前端 `Community.vue`；管理端已有成熟审核范式（`AdminAuditController` + `Review.vue` 双分区 + `api.auditCourse/auditResource`）。

### 5.2 逐项冲突分析

| 优化动作 | 潜在冲突 | 判断依据 | 应对策略 |
|---|---|---|---|
| WP1 改 jwt-secret 为 `${JWT_SECRET:默认}` | ①部署时设新密钥 → 所有已签发 token 失效，在线用户被登出；②`JwtUtil` 用 HmacShaKeyFor 要求密钥 ≥32 字节，用户自设过短密钥会启动报错 | jjwt HS256 密钥长度约束；本项目 token 有效期 72h（application.yml:74） | ①影响可接受（发布前无存量用户），在 README 标注"更换密钥会使所有登录失效"；②`.env.example` 注释注明"随机字符串 ≥32 字符"，并在 README 给出生成命令示例（`openssl rand -base64 32`） |
| WP2 detail 加 userId 归属校验 | ①方法签名变化需同步调用方 Controller；②前端"我的项目/我的计划"页是否有他人数据入口 | 全库仅各自 Controller 一处调用 detail（评估报告已核）；前端列表均来自 listMine（自建列表），不存在展示他人详情的正常路径 | ①Controller 改传 `UserContext.getUserId()`；②前端零改动，非归属者访问只会得到 403 错误 toast，属正确行为 |
| WP3 把 content 列 + FULLTEXT 索引并入 init_learning.sql | **关键冲突**：Docker 首建时 init_learning 先建好索引，随后按字母序执行的 `upgrade_fulltext_index.sql`（裸 ALTER，无守卫）会报 `Duplicate key name`，而 **docker-entrypoint-initdb.d 中任一脚本执行出错会中断整个 MySQL 容器初始化**，导致一键部署失败 | mysql 官方 entrypoint 行为：initdb 脚本非零退出即中止；`upgrade_fulltext_index.sql:6` 无 information_schema 守卫（评估报告中风险 #14，此处升级为前置依赖） | **WP3 必须捆绑**：给 `upgrade_fulltext_index.sql` 加与 `upgrade_news_fulltext.sql:6-17` 相同的 information_schema 守卫（列存在则 SELECT 1 跳过）。守卫后：新库 Docker 路径 init 建索引→upgrade 跳过；老库手动路径 upgrade 正常补建；已建索引库重复执行无害 |
| WP3 同类冲突检查：upgrade_guest_public_news.sql | init_learning 已建 knowledge_news（含新 content 列）后，该脚本 `CREATE TABLE IF NOT EXISTS` 跳过 | 已核脚本内容（IF NOT EXISTS 守卫） | 无冲突，不改 |
| WP3 同类冲突检查：upgrade_news_fulltext.sql | init_learning 建表已含 content 列后，该脚本 information_schema 守卫跳过 ALTER | 已核脚本自带守卫（:6-17） | 无冲突，不改 |
| WP4 compose 补 NEWS_RSS_FEEDS | 变量值含逗号与 `\|` 分隔符，YAML 环境变量传递需注意格式 | .env.example/README 中该变量格式约定 | compose 中整体作为单个字符串传递（`NEWS_RSS_FEEDS: ${NEWS_RSS_FEEDS:-}`），Spring 侧按逗号切分逻辑不变（NewsService 已有实现），无结构冲突 |
| WP5 createPost 改 auditStatus=0 | ①**连锁影响最大项**：feed SQL 过滤 audit_status=1 → 发帖人自己发帖后在 feed 中看不到，体验断裂且答辩演示无法现场展示"发帖→列表出现"；②detail(postId) 不过滤 audit_status，未过审帖可被直达链接访问；③已有存量帖子 audit_status=1 不受影响；④对未过审帖的回答/评论不受限制（保持开放） | `PostMapper.listFeed` 的 WHERE 条件（已核）；Community.vue 无"我的帖子"独立页（已核） | ①`listFeed` SQL 增加分支 `OR p.user_id = #{userId}`，feed 方法签名加当前登录 userId——发帖人自己始终可见自己的帖子（含待审），他人在审核通过前不可见；②detail 保持可访问（发帖人可预览待审内容），**返回中携带 auditStatus 由前端打"待审核"标记**——此为方案内定口径，请确认（见第八节 U2）；③无迁移需要；④标注为已知取舍写入 README |
| WP5 AdminAuditController 加 decidePost | 该 Controller 现直接注入 4 个 Mapper（评估报告中风险 #1 分层问题），本次按既有模式加第 5 个 Mapper 而不下沉 Service——与"规范一致性"阻断标准存在轻微张力 | 保持改动最小 vs 规范整改 | 明确决策：**本次从众既有模式**（PostMapper 注入 + 与 decideCourse 同构的 decidePost 方法），分层重构留给后续批次；该取舍在提交说明与 README 中注明 |
| WP6 chroma 加 healthcheck | chroma 1.0.0 镜像内可用探测命令不确定（wget/curl 是否存在）；heartbeat 路径需实测（`/api/v2/heartbeat` 为候选）；healthcheck 命令错误会导致 backend 因 `service_healthy` 永不启动，比现状（竞态重启）更糟 | 镜像基于 python；API 版本随大版本变化 | 实测时先 `docker compose exec chroma python -c "import urllib.request;urllib.request.urlopen('http://localhost:8000/api/v2/heartbeat')"` 验证路径可用，再落 healthcheck；若路径不可用退化为 TCP 端口探测；**healthcheck 与 depends_on 升级绑定实测通过后提交**，避免引入比竞态更重的故障 |
| WP6 修改 README | 与评估报告 H9（端口 5173/5174 失实）叠加，同一次 README 改动可顺手修复 | 同一文件 | 建议并入（一行改动），见第八节 U1 待确认 |
| WP1/WP4 与测试的关系 | 新增测试不引用 jwt-secret（已 grep 核实 `backend/src/test/` 无 jwtSecret 引用）；AuthInterceptorTest 不经过密钥签名路径 | 已核测试源码引用 | 无冲突 |

### 5.3 冲突分析结论

唯一**必须捆绑处理的硬冲突**是 WP3 的"init_learning 合并索引 ↔ upgrade 脚本无守卫 → Docker 初始化中断"链（5.3 表第 3 行）；其余冲突均有明确应对且不阻断。社会性/体验类冲突集中在 WP5 的 feed 可见性，已给出"发帖人自见"方案。

---

## 六、优化方法（分工作包）

### 6.1 WP1：JWT 密钥环境变量化（对应 P0-1，目标 G1）

1. `application.yml:73` 改为 `jwt-secret: ${JWT_SECRET:rag-edu-graduation-project-jwt-secret-please-change-in-prod}`（保留默认值保证本机开发零配置可跑，发布时通过环境变量覆盖）。
2. `.env.example` 增加 `JWT_SECRET=`（注释：随机字符串 ≥32 字符，`openssl rand -base64 32` 生成；更换后所有已登录用户失效）。
3. `docker-compose.yml` backend environment 增加 `JWT_SECRET: ${JWT_SECRET:-rag-edu-graduation-project-jwt-secret-please-change-in-prod}`。
4. README「常用环境变量」表补 `JWT_SECRET` 行。
- **方法依据**：与同文件 `rerank-api-key: ${RERANK_API_KEY:}` 既有占位模式一致（评估报告 P0-1 指出"同文件其他配置均有占位"）。
- **前提/风险**：需发布者记得在 .env 中设置强密钥——通过 README 部署 checklist 提示；不设置时仍用仓库默认值（安全性依赖发布者动作，已在 README 标注为"对外部署必改项"）。

### 6.2 WP2：detail 越权修复（对应 P0-2/D-1，目标 G2）

1. `ProjectService.detail(Long projectId)` → `detail(Long projectId, Long userId)`：查到后校验 `pc.getUserId().equals(userId)`，不匹配抛 `BizException(403, "无权查看该项目")`（与同文件 delete:87-89 的既有校验写法一致）。
2. `StudyPlanService.detail(Long planId)` → `detail(Long planId, Long userId)`：同样比对（参照同文件 verifyPlanOwner:109-114）。
3. 对应 Controller（ProjectController:29-31、StudyPlanController:28-31）传入 `UserContext.getUserId()`。
4. 新增测试（并入 WP 测试包）：两服务各 2 条用例（归属者 200 / 非归属者 403）。
- **方法依据**：评估报告 P0-2 建议"补归属校验（比对 UserContext.getUserId()）"；项目内已有成熟同构写法可直接复制。
- **风险**：低——调用方唯一、前端无他人详情入口（5.2 表已论证）。

### 6.3 WP3：SQL 建库脚本收敛（对应 P0-3/P0-4，目标 G3）

1. `sql/init_learning.sql`：①knowledge_news 建表语句增加 `content LONGTEXT NULL COMMENT '资讯正文(纯文本,已标注出处)'`（置于 summary 之后，与 upgrade_news_fulltext.sql:12 定义逐字一致）；②doc_chunk 建表语句增加 `FULLTEXT INDEX ft_doc_chunk_content (content) WITH PARSER ngram`（与 upgrade_fulltext_index.sql:6 一致）。
2. `sql/upgrade_fulltext_index.sql`：套用 `upgrade_news_fulltext.sql:6-17` 的 information_schema + PREPARE/EXECUTE 守卫（索引已存在则 SELECT 1 跳过）。**此步与 1 强制捆绑**（冲突分析 5.3）。
3. `README.md` 手动部署步骤 1 简化为：新库仅执行 `mysql -u root -p < sql/init_learning.sql`；补充说明"老库升级按需执行 sql/upgrade_*.sql（均幂等可重复执行）"。
4. `upgrade_guest_public_news.sql`/`upgrade_news_fulltext.sql` 不动（已幂等）。
- **验证**：空库执行 init_learning.sql → 三项检查（content 列/索引/资讯抓取）；再重复执行两个 upgrade 脚本验证幂等不报错。
- **风险**：老库已手工建过索引 + 又执行守卫版 upgrade → 跳过，无副作用；唯一注意点是 MySQL information_schema 对 FULLTEXT 索引的探测写法（`STATISTICS` 表 `INDEX_TYPE='FULLTEXT'`），实现时以实测为准。

### 6.4 WP4：compose 环境变量补齐（对应 P0-5，目标 G4）

1. backend environment 块追加（均带默认值，未设置时行为与现状一致）：
   `JWT_SECRET`（随 WP1）、`QUOTA_ENABLED`、`QUOTA_CHAT_LIMIT`、`QUOTA_GENERATE_LIMIT`、`QUOTA_GRADE_LIMIT`、`QUOTA_PROJECT_LIMIT`、`QUOTA_GRAPH_LIMIT`、`NEWS_FETCH_CRON`、`NEWS_RSS_FEEDS`。
2. `.env.example` 补 `NEWS_FETCH_CRON`/`NEWS_RSS_FEEDS` 注释示例（含 `\|` 分隔格式说明）。
3. README 环境变量表补 `RERANK_API_KEY`（评估报告后端质量审查 M7 指出该变量三处配置已有但表格缺失，一并补齐）。
- **依据**：评估报告 P0-5 建议逐字对应；变量名与 application.yml 占位符一一核对。
- **风险**：无行为风险（默认值兜底）；仅 YAML 格式注意（NEWS_RSS_FEEDS 单字符串传递）。

### 6.5 WP5：社区帖审核流（对应 D-2，目标 G5）

后端：
1. `CommunityService.createPost`：`post.setAuditStatus(0)`（待审核），删除"演示:直接通过"注释。
2. `PostMapper.listFeed`：SQL 增加 `<if test="userId != null"> OR p.user_id = #{userId} </if>` 分支（外层括号包裹 OR 优先级），`feed(courseId, type, userId)` 签名加 userId；`CommunityService.feed` 传入 `UserContext.getUserId()`（社区为登录后功能，userId 恒非空）。
3. `AdminAuditController`：①`pending()` 增加 posts 段（`audit_status IN (0,2)` 的帖子：标题/作者昵称/类型/审核状态/时间，复用 nicknamesOf）；②新增 `@PostMapping("/post/{postId}")` decidePost（与 decideCourse:91-100 同构）。
4. `CommunityService.detail` 返回的 post 本身含 auditStatus（entity 字段已有），不改后端。

前端：
5. `Review.vue`：按现有"课程/教材"两分区模式增加"社区帖子"第三分区（标题、作者、类型标签、审核状态、通过/驳回按钮）。
6. `api/index.js`：新增 `auditPost(postId, action)`，风格对齐 auditCourse/auditResource。
7. `Community.vue`：发帖成功提示改为"已提交，待管理员审核后对他人可见"；帖子卡片若 `auditStatus !== 1` 显示"待审核/未通过"标记（数据已随 feed 返回）。

测试：
8. 新增 CommunityService 审核用例：新帖 auditStatus=0；listFeed 他人视角不含待审帖、本人视角含；decidePost approve 后 feed 对他人可见（单测直接调 Controller 方法 + mock mapper，与项目现有单测风格一致）。
- **依据**：评估报告 D-2 用户裁定"补审核流"；实现模式完全复用课程/教材审核既有范式（AdminAuditController 注释、Review.vue 分区、api 封装三处同构）。
- **风险**：答辩演示流程变化——现场发帖需管理员账号即时审核才对他可见；建议演示脚本改为"预置已过审帖子 + 现场演示一次审核动作"，或用 admin 发帖。README 验收流程第 7 步同步补充。

### 6.6 WP 测试包：越权 + 审计用例（对应 D-5，目标 G7）

1. 新增 `ProjectServiceTest`（4 例：detail 归属/越权、listMine 隔离、delete 越权拒绝）。
2. 新增 `StudyPlanServiceTest`（4 例：detail 归属/越权、addTask 越权拒绝、delete 越权拒绝）。
3. 新增审计测试（`AdminAuditControllerTest`，直接实例化 Controller + mock mapper）：decideCourse approve/reject、decideResource 非法 action 抛 BizException、decidePost 通过后帖子可见（WP5 第 8 条合并至此）。
- **风格依据**：对齐现有 6 个测试类的纯单测 + Mockito 模式（评估报告第六节已核）。
- **工作量依据**：合计新增 ≥ 12 例，G7 阈值 40 例可达。

### 6.7 WP6：Docker 实测 + healthcheck + README 收口（对应 P0-6/H9/D-3/4/6/9，目标 G6）

1. chroma healthcheck：实测确认 heartbeat 端点后，compose chroma 服务块加 healthcheck，backend depends_on 升级为 `chroma: condition: service_healthy`（探测命令实现细节见 5.2 冲突表）。
2. 完整实测：`docker compose down -v && docker compose up -d --build`（空卷首建场景）→ README 七步验收（登录/上传/问答引用/计划打卡/出题评分/项目辅导/管理端）→ 记录验收结果；再 `docker compose restart backend` 验证已有数据场景。
3. README 收口（同一文件一次改完）：①端口 5173→5174 修正（H9，待确认 U1）；②手动部署步骤（WP3 第 3 条）；③环境变量表补 JWT_SECRET/RERANK_API_KEY/NEWS_*；④新增「设计与安全取舍说明」小节：D-3（未登录可读公共内容大部分功能，登录为个性化）、D-4（?token= URL 为文件预览保留的取舍）、D-6（主观题 AI 评分失败降 0 分待人工复核）、D-9（Redis 故障配额 fail-open）；⑤验收流程补社区审核演示步骤。
4. 知识回流：实测结论（含 chroma 镜像兼容性、registry-mirror 配置是否生效）更新 `docs/实现进展.md` 与个人知识库。

### 6.8 实施顺序与提交拆分（遵循 PROJECT_CONVENTIONS）

| 序 | 动作 | 提交类型 | 前置 |
|---|---|---|---|
| 1 | WP2 代码修复 + 越权测试 → `mvn -o compile && mvn -o test` | `fix(security): 项目辅导与学习计划详情接口补归属校验` | 无 |
| 2 | WP1 JWT + WP4 compose/.env.example → 编译 | `fix(security): JWT密钥支持环境变量覆盖,compose补齐配额与资讯变量` | 1 |
| 3 | WP3 SQL 收敛 → 空库脚本验证 | `fix(sql): 新库脚本并入资讯正文列与全文索引,升级脚本加幂等守卫` | 2 |
| 4 | WP5 审核流（前后端）+ 审计测试 → 后端编译测试 + 前端 build | `feat(community): 社区帖发布进入审核流,管理端支持帖子审核` | 3 |
| 5 | WP6 实测 + healthcheck + README 收口 | `fix(deploy): chroma健康检查与依赖编排,README部署步骤与取舍说明修正` | 4 |
| 6 | （实测通过后）`docs(rag): 发布前优化实施与Docker实测记录` | 5 | 5 |

预计总工作量：**2.5～3 个工作日**（WP2/3/4 合计约 1 天，WP5 约 1 天，WP6 实测约 0.5-1 天）。

---

## 七、验证与放行标准（对应四项阻断标准）

| 阻断标准 | 发布放行条件 |
|---|---|
| 可运行性 | G3 + G6 全部成立（空库单脚本建库两条链路均正常 + Docker 实测七步验收通过） |
| 安全底线 | G1 + G2 成立（密钥可覆盖 + 两处 IDOR 修复且有回归测试） |
| 测试与回归 | G7 成立（`mvn -o test` 全绿 ≥ 40 例，含新增越权/审计用例；`npm test` 6/6） |
| 代码规范一致性 | 六次提交全部 Conventional Commits 单一职责；WP5 分层取舍在提交说明注明；编译/构建全过 |

---

## 八、待确认事项（U 系列，不阻塞方案其余部分）

| # | 事项 | 方案默认口径 | 需确认原因 |
|---|---|---|---|
| U1 | H9 端口失实（README 5173 vs 实际 5174）是否随 WP6 一并修 | 默认修（一行，直接影响答辩演示） | 用户裁定范围"仅 P0"，此为高风险项，需授权纳入 |
| U2 | 未过审帖的详情接口可直达（detail 不过滤 audit_status） | 默认保持可访问（发帖人预览自己的待审帖），前端打"待审核"标记 | 涉及"社区内容公开口径"的产品决策：若要求未过审帖完全不可见，detail 也需校验，改动+1 |
| U3 | chroma heartbeat 探测命令 | 默认 `/api/v2/heartbeat`，实测后定 | chroma 1.0.0 API 版本未经实测（评估报告部署审查不确定项 1） |
| U4 | D-7 发布前是否轮换本机 .env 中的真实 DeepSeek Key | 建议轮换（运维动作，10 分钟） | 仅提醒，无代码工作量 |

---

## 附：依据索引

- 评估报告问题编号：P0-1~P0-6（第二节）、H1-H10（第三节）、中风险 1-17（第四节）、D-1~D-9（第七节）——本文所有"依据"均可回溯至该报告对应条目及其中标注的文件:行号。
- 用户裁定记录（2026-09-14）：范围=仅 P0；D-2=补审核流；D-5=补审计+越权用例；D-3/D-4/D-6/D-9=设计意图不改码。
- 冲突分析中"docker-entrypoint-initdb.d 脚本报错中断初始化"为本方案编制期间新核明的约束（mysql 官方 entrypoint 行为 + `upgrade_fulltext_index.sql` 无守卫），非评估报告原有结论。
