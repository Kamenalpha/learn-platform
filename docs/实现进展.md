# 实现进展(Implementation Progress)

> 面向「通用多学科智能学习平台」毕设,记录已完成的功能模块、代码位置与论文章节映射,供写作与验收对照。

## 一、总体状态

- 后端:Spring Boot + Spring AI,已迁移至新库 `learn_platform`(34 张表),**离线编译与 13 个服务回归测试通过(JDK17)**。
- 前端:Vue3 + Element Plus 响应式,核心页面/API/路由/菜单已就绪,**`npm run build` 已通过**。
- 基础设施:MySQL `learn_platform` 已建库导入;Chroma 已用项目 venv(`.venv` 装 `chromadb`)在 127.0.0.1:8000 运行(数据 `data/chroma`);Redis(6379)与 LLM/Embed API Key 需另行提供(见 `scripts/start-infra.ps1`)。

## 二、后端模块(controller / service)

| 模块 | 接口前缀 | 主要文件 | 说明 |
|------|----------|----------|------|
| 认证 | `/api/auth` | `AuthController` | 注册/登录,角色(用户/管理员) |
| 学科 | `/api/subjects` | `SubjectController` | 分类顶层 |
| 课程 | `/api/courses` | `CourseController` | 二级分类,用户归属+公开可见 |
| 章节 | `/api/chapters` | `ChapterController` | 三级分类 |
| 知识点 | `/api/knowledge-points` | `KnowledgePointController` | 四级分类叶子 |
| 资源(资料) | `/api/docs` | `DocumentController` | 上传(PDF/Word/PPT/TXT+OCR)/解析/分块/预览/重解析/删除 |
| AI 助手 | `/api/assistants` | `AssistantController` | 绑定课程;风格/上下文轮数/引用开关生效;自定义提示词不能覆盖知识库约束;模型/温度为部署级配置 |
| 问答 | `/api/chat` | `ChatController` | 同步+SSE 流式 RAG 问答,课程隔离,Redis 历史按 userId+sessionId 隔离,可选引用溯源 |
| 学习计划 | `/api/plans` | `StudyPlanController` | 计划+任务,完成打卡埋点 |
| 社区 | `/api/community` | `CommunityController` | 帖子/问答/采纳/评论/点赞 |
| 出题模拟 | `/api/exam` | `ExamController` | 选项 JSON 数组;正式考试答案隔离;真实起止时间/服务端限时/原子防重复交卷;客观+AI 判分/错题本;错题一键变式训练(source_type=3 轻量卷,不限时) |
| 项目辅导 | `/api/projects` | `ProjectController` | AI 拆解→方案/任务/阶段/报告 |
| 学习画像 | `/api/analytics` | `AnalyticsController` | 时长/连续打卡/弱项统计;AI 诊断(评分评语沉淀) |
| 管理后台 | `/api/admin/*` | `Stats/Kb/User` 等 | 用户/审核/RAG参数/看板;成本配额已执行(QuotaService,超限拒绝,用量看板) |

## 三、前端页面(views)

`KnowledgeBase`(四级树+资料)、`Assistant`(对话+管理)、`StudyPlan`、`Community`、`Exam`(出题/考试/错题)、`Project`、`Analytics` 等;另有 `Login` 及管理端(`admin/`)。

## 四、数据库

- `sql/init_learning.sql`:独立新库 `learn_platform`,34 张表,覆盖 分类/资料/分块/助手/计划/社区/题目/试卷/考试/错题/项目/画像/审核/分享/AI观察。
- 旧库 `rag_edu`(原 RAG 系统)保留未动;映射关系:`course_document→resource`、`doc_chunk`(doc_id→resource_id)、`qa_record`(+assistant_id)。

## 五、论文章节映射

| 论文章节 | 对应实现 |
|----------|----------|
| 第3章 需求分析 | `毕设.md` 第1节(用例图/规约/流程) |
| 第4章 系统总体设计 | `毕设.md` 第4节(架构/ER/表清单) |
| 第5章 详细实现 | 本文「二、三、四」各模块 |
| 第6章 系统测试 | 已有 19 个后端服务回归测试、前端生产构建与引用拆分单测(`npm test`);仍需补真实 AI 全链路、性能和问答效果报告 |
| 第7章 总结展望 | 待写 |

## 六、待实现 / 优化项

- 学习画像数据埋点:目前仅"做题/打卡"接入,问答/浏览/项目等行为可补充。
- 分数线/难度曲线、出题质量评估、撤回/二次生成。
- OCR(PaddleOCR 服务)需外部部署,未配置时扫描件解析为空。
- 移动端 Vant 组件强化(目前 Element Plus 响应式)。
- 全文搜索(资料/帖子/问答/题目)。
- AI 助手模板、按助手模型/温度切换。
- ~~Docker 一键部署文件已就绪但未实测(本机无 Docker)~~ → 2026-09-14 已全链路实测通过(见下「发布质量加固」节)。
- RAGAS 检索/生成质量评估(后续优化实施方案 步骤9,暂缓)。
- 引用 PDF.js 文本层高亮定位(二期;一期锚点跳页已完成,见「已完成」)。

## 七、已完成(2026-09-14 发布质量加固与 Docker 实测)

> 依据《RAG发布质量评估报告-20260914》P0 清单执行《RAG发布质量优化方案-20260914》(WP1–WP6,6 笔提交),TDD 流程:每阶段先写测试再实现,后端测试 34→55 例全绿。

- **IDOR 修复(ae5cb35)**:项目辅导/学习计划详情接口此前任意登录用户可按 ID 翻查他人数据,现 `ProjectService.detail`/`StudyPlanService.detail` 补归属校验(非本人 403,不存在 404),delete 同步收紧;新增 `ProjectServiceTest` 5 例、`StudyPlanServiceTest` 4 例。
- **JWT 密钥外置(bf1a070)**:`jwt-secret` 支持 `${JWT_SECRET:默认}` 覆盖,compose/.env.example 同步;实测 G1:更换密钥后旧 token code=401、新登录正常。
- **SQL 收敛(823aeb9)**:`init_learning.sql` 并入 knowledge_news.content 列与 doc_chunk 的 ngram 全文索引(新库一条脚本建齐),`upgrade_fulltext_index.sql` 加 information_schema 守卫可重复执行;本机临时库实测索引生效且升级幂等。
- **社区审核流闭环(b7175f3)**:帖子发布即 `audit_status=0` 进入审核,feed 只返回已过审帖+本人帖(待审/未过审帖对他人不可见);管理端审核页新增"社区帖子"标签,`POST /api/admin/audit/post/{id}?action=` 裁定;新增 CommunityServiceTest 3 例、AdminAuditControllerTest 8 例。
- **TXT 问答必崩修复(fa2fd2e)**:评估报告之外的实测发现——TXT 文档无页码,IngestService 写入 `page=""`,检索时 `Integer.valueOf("")` 抛 NumberFormatException 导致该文档全部问答 500;`RetrievalService` 改为容忍空 page 并补回归测试。
- **部署编排加固(9171606)**:chroma 1.0.0 为 Rust 单二进制镜像(无 python/curl),healthcheck 改用 bash `/dev/tcp` 原生 HTTP 探测 `/api/v2/heartbeat`,backend 依赖升级为 `service_healthy`;README 修正端口(前端 5174)、补环境变量表与「设计与安全取舍说明」(D-3/D-4/D-6/D-9 有意为之项)。
- **Docker 全链路实测(16/16)**:四容器 healthy;API 验收覆盖登录、建课、TXT 上传→解析→向量化(chunk_count=1)→问答(5 条引用)、学习计划+打卡、出题(3 题)、项目辅导、社区完整审核链(发帖→他人不可见→本人可见→管理端待审→过审→他人可见)、管理看板、IDOR 403;`.env` 空 `NEWS_RSS_FEEDS` 字符串会压掉 YAML 默认值的坑已用 compose 兜底消除。
- 环境实测后已还原:compose 栈与卷清理、本机 MySQL(3306)服务恢复运行。
- 发布前提醒:对外部署必须更换 `JWT_SECRET`(README 八节)并轮换真实 LLM/Embed Key;答辩演示社区功能需先过审或预置已过审帖。

## 七、已完成(2026-09-10 Docker 一键部署)


- 新增 `backend/Dockerfile`(多阶段:maven 依赖层 + 打包 → JRE 17 运行,UTF-8/上海时区)与 `backend/.dockerignore`;新增 `.env.example`(API Key/数据库密码/配额模板,`.env` 已在 .gitignore)。
- `docker-compose.yml` 升级:新增 `backend` 服务(构建 ./backend,依赖 mysql/redis healthcheck 后启动,注入 LLM/EMBED/DB/Redis/Chroma 环境变量,上传目录挂命名卷);mysql/redis 增加 healthcheck;`MYSQL_DATABASE` 由旧的 rag_edu 更正为 learn_platform。
- `sql/upgrade_fulltext_index.sql` 补 `USE learn_platform`(Docker 首次初始化按字母序执行全部 sql 且无默认库,否则 ALTER 报"No database selected"中断建库)。
- README 快速开始拆为「方式 A:Docker 一键部署」与「方式 B:手动部署」。
- ~~注意:本机未安装 Docker,以上文件未实际构建/运行验证~~ → 2026-09-14 已实测(四容器 healthy、16 项 API 验收全过),详见下「2026-09-14 发布质量加固」节。

## 七、已完成(2026-09-10 AI 学情观察)

- 新表 `ai_diagnosis`(user_id + kp_id + source_type + content + ref_id,`uk_ref` 唯一防重;`sql/upgrade_ai_diagnosis.sql` 老库升级脚本,`init_learning.sql` 同步,总表数 33→34)。
- 写入:主观题 AI 评分成功且评语有效时,`ExamPracticeService.grade` 落库作答后顺手写入观察(评分失败的占位评语跳过;写入失败仅告警不影响判分);问答误解来源(source_type=1)二期预留。
- 读取:`GET /api/analytics/diagnoses` 返回本人最近 20 条(带知识点名,无知识点显示"通用观察");Analytics.vue 新增"AI 诊断"卡片。
- 测试:ExamPracticeServiceTest 新增观察写入门控用例(有效评语入库/失败占位与空评语跳过)。

## 七、已完成(2026-09-10 成本配额执行)

- `QuotaService` 统一出口埋点:调用前 `checkQuota`(超限抛 429 业务异常)、成功后 `record`(Redis `rag:quota:{user}:{action}:{yyyyMM}` INCR+首次过期到月底;`quota_log` `ON DUPLICATE KEY UPDATE` 原子累加)。管理员不限量;Redis 异常检查放行(可用性优先),记账失败仅告警。
- 覆盖 5 类动作:chat 问答(同步+流式,流式在流结束落库后记账)、generate 出题、grade AI 评分、project 项目辅导、graph 知识图谱。默认额度(可环境变量覆盖):200/30/200/10/30 次/月,`QUOTA_ENABLED=false` 整体关闭。
- 管理端看板:`GET /api/admin/stats/ai-usage`(当月按动作汇总/Top10 用户/最近 50 条明细),Stats.vue 新增"AI 用量与配额"卡片。
- 越权回归:新增 `AuthInterceptorTest`(普通用户访问 /api/admin/** 403、管理员放行、非 admin 接口不受影响)。
- 测试:后端全绿(新增 QuotaServiceTest 8 例 + AuthInterceptorTest 3 例),README/进展文档"配额未执行"清零。

## 七、已完成(2026-09-10 错题变式训练)

- 错题本「变式训练」按钮:`POST /api/exam/mistakes/{id}/variants`,取原题+该生最近一次判错作答+解析,LLM 结构化输出 3 道同考点变式题(换数字/情境/问法,复用 `GeneratedQuestion` record 与出题链路,不走知识库检索),自动组 `paper.source_type=3` 轻量练习卷(不限时,每题 10 分)。
- 越权防护:错题归属校验(非本人 404);LLM 失败包装为业务异常不阻断;作答/判分/错题回收走既有链路,形成"错→练→再错"闭环。
- 前端 Exam.vue:错题本操作列「变式训练」按钮(防连点),生成后切到出题页预览作答;考试记录列表变式卷显示标签。
- 测试:后端 22 例全绿(ExamPracticeServiceTest 新增建卷/越权/LLM 失败包装 3 例),前端生产构建与引用拆分单测通过。

## 七、已完成(2026-09-10 引用跳转原文)

- 引用抽屉 `CitationDrawer.vue`:Chat/Assistant 两页回答中的 [n] 编号渲染为可点击 chip,点击打开抽屉并定位到对应来源,展示文档名/页码/检索得分/原文片段;来源列表卡片/标签同样入口。
- 跳转原文:文件直链 `/api/docs/{docId}/file?token=` + `#page=N` 锚点,由浏览器自带 PDF 阅读器原生跳页;无页码的块(TXT/Word)降级为只展示片段、不显示跳转按钮。
- 补齐 `api.docFileUrl` 封装(文档管理页「原文件」按钮此前调用了不存在的封装会报错,已顺带修复);Assistant 页原 blob 打开方式一并统一为直链。
- 后端:引用来源装配抽为 `ChatService.buildSourceContext`(包级可见,`Source` 的 docId/page/chunkId/snippet 即前端跳转的数据契约,同步/流式共用)。
- 测试:后端 19 例全绿(ChatServiceTest 新增溯源字段装配 2 例);前端新增零依赖 Node 内置测试 `npm test`(引用编号拆分 6 例,覆盖超范围编号/半截流式文本降级),生产构建通过。

## 七、已完成(2026-09-09 检索质量升级)

- 混合检索:Chroma 向量 + MySQL ngram 关键词双路召回,RRF 融合(管理端开关重排;`RetrievalService` 统一入口,问答/出题共用)。
- BGE-Reranker 重排:兼容 `/v1/rerank`,`RERANK_API_KEY`(默认复用 `EMBED_API_KEY`),失败自动降级融合顺序;未建全文索引时自动降级仅向量。
- 管理端「检索测试」页(`/admin/kb-debug`):展示关键词分/向量分/融合分/重排分,可临时覆盖参数。
- 出题与主观题 AI 评分改用 Spring AI 结构化输出(`entity()`),删除手写 JSON 抠取。
- 测试:17 个服务回归测试全绿(新增 RetrievalServiceTest 4 例),前端生产构建通过。

## 七、项目约束

见 `项目规范.md`(原 PROJECT_CONVENTIONS.md,2026-09-15 更名并升级分支管理规则):修改前先提交、修改后编译通过再提交、Conventional Commits、大改动走分支。
