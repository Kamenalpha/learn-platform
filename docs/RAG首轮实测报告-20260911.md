# RAG 首轮实测报告（2026-09-11）

> 实测环境：本机手动部署（方式 B）。MySQL 8.0.46 + Chroma(venv,数据 `data/chroma`)+ Redis(便携版)+ 本地 ONNX 嵌入服务(`scripts/embed_server.py`,BAAI/bge-small-zh-v1.5,端口 9100)+ DeepSeek `deepseek-chat`。基准脚本 `scripts/bench_rag.py`,检索分阶段诊断 `scripts/diag_retrieval.py`。

## 1. 文档量级

| 指标 | 数值 | 说明 |
|------|------|------|
| 教材文档 | 5 个 PDF | 全部解析成功(parse_status=1) |
| — OSTEP | 3 章 / 221 块 | 01-Process(64)、02-Limited-Direct-Execution(102)、03-Address-Spaces(55) |
| — MIT 18.01 | 2 章 / 15 块 | 01-Rates-and-Derivatives(8)、02-Slope-Limits-Continuity(7) |
| 分块总数 | 236 | `doc_chunk` 表 |
| Chroma 向量 | 236 | 集合 `learn_platform_knowledge`,与分块数一致 |
| 每日资讯 | 34 条 | `knowledge_news` |
| 问答记录 | 实测前 2 条 → 实测后 16 条 | 本轮 14 问(6+8)全部落库 |
| AI 用量记账 | 本月 chat ×14 | `quota_log` 记账与请求数一致 |

**检索链路当前状态(重要)**:
- 关键词路 **未生效**:`doc_chunk` 无 ngram 全文索引(`sql/upgrade_fulltext_index.sql` 未执行),混合检索实际退化为单向量路(debug 接口 kwScore 全为 null)。
- 重排 **未生效**:管理端开关 `rerankEnabled=true`,但 `RERANK_API_KEY` 未配置(application.yml 默认为空,为空即安静关闭,不会误调用)。

## 2. 响应延迟(8 题)

| 题目 | 客户端耗时 | 服务端 elapsedMs | 结果 |
|------|-----------|-----------------|------|
| OSTEP-进程(中) | 3728 ms | 3694 ms | ✅ 5 条引用 |
| OSTEP-受限直接执行(中) | 571 ms | 558 ms | ❌ 空召回 |
| OSTEP-地址空间(中) | 813 ms | 779 ms | ❌ 空召回 |
| MIT18.01-导数(中) | 3563 ms | 3528 ms | ✅ 5 条引用 |
| MIT18.01-极限(中) | 944 ms | 908 ms | ❌ 空召回 |
| OSTEP-地址空间(英) | 2584 ms | 2570 ms | ✅ 5 条引用 |
| OSTEP-LDE(英) | 2843 ms | 2829 ms | ✅ 5 条引用 |
| 越界-量子计算(中) | 2059 ms | 2033 ms | 🛡 正确拒答 |

- 有效回答(检索+LLM 生成 ~600-1100 字):**2.6–3.7 s**;空召回拒答:**0.6–2.1 s**。
- 全部 8 题:min 571 ms / p50 2584 ms / avg 2138 ms;客户端与服务端耗时差 <100 ms(本机网络开销可忽略)。

## 3. 检索引用命中表现

- **中文问 5 题:命中 2/5(40%)**。空召回 3 题的知识(受限直接执行/地址空间/极限)实际都在库中。
- **英文问 2 题:命中 2/2**,引用文档全部正确(OSTEP-03、OSTEP-02),页码可用。
- **越界问 1 题:正确拒答**(防编造规则起效),但 references 仍带回 5 条不相关分块(过了阈值的噪声块进入了上下文),前端会显示无关来源。
- 引用标注一致性:答案中 `[n]` 标记数 ≈ references 条数(5/5、5/5、4/5、3/5),可追溯性正常。
- references 的 score 字段为 RRF 融合分(~0.016)而非向量相似度,前端若直接展示"相关度"会误导。

## 4. 空召回根因(经 debug 接口分阶段验证)

`GET /api/admin/kb/debug?question=...` 对照实验:

| 用例 | 召回 | 向量分 |
|------|------|--------|
| 中文问 LDE(阈值 0.5) | **0 条** | 全部 <0.5 |
| 英文问 LDE(阈值 0.5) | 20 条 | 0.63–0.68 |
| 中文问 LDE(阈值 0.3) | 20 条 | 0.486–0.495 |
| 英文问地址空间(阈值 0.5) | 20 条 | 0.636–0.683 |
| 中文问极限(阈值 0.3) | 20 条 | 0.395–0.41 |

根因三条,按贡献排序:
1. **跨语言向量分系统性压低**:bge-small-zh 对"中文问/英文档"的相似度集中在 0.40–0.53,比同概念英文问低约 0.15。
2. **阈值 0.5 恰好卡线**:中文问分数带上沿 ≈0.52,阈值 0.5 把整批结果砍成 0;降到 0.3 则 20 条全出。
3. **关键词路缺位**:query 含英文术语(如 "Limited Direct Execution")本可由 ngram 全文索引兜底,但索引未建,双路退化为单路,失去容错。

## 5. 修复建议(未执行,待决策)

1. 执行 `sql/upgrade_fulltext_index.sql` 建 ngram 全文索引,恢复混合检索双路兜底(对含英文术语的中文 query 直接有效)。
2. 默认相似度阈值 0.5 → 0.35~0.4,并开启重排(`RERANK_API_KEY` 复用嵌入 Key)精排补偿,兼顾噪声过滤。
3. 长期:教材以英文为主时,换多语言嵌入模型(bge-m3 / text-embedding-v3),注意换模型须删除旧 Collection 全量重建向量(README 七)。
4. 越界场景的 references 噪声:生成侧已正确拒答,可考虑回答为"暂无相关内容"时不返回 references,或按重排分二次过滤。

---

# 优化执行与复测（2026-09-12）

按上述建议执行了 P0+P1 共 4 项（P2 开重排/查询双语化、P3 换嵌入模型留待决策）：

| 项 | 改动 | 验证 |
|----|------|------|
| P0-1 建全文索引 | 执行 `sql/upgrade_fulltext_index.sql`(`ft_doc_chunk_content`,ngram) | debug 接口中文问 kwScore 恢复(如 LDE 题 kw=2.75/3.80),双路命中的块 RRF 分翻倍(0.033 vs 单路 0.016) |
| P0-2 阈值下调 | `application.yml` `default-threshold: 0.5→0.4`(持久化默认值;管理端 Redis 覆盖仍优先) | 中文问地址空间/极限分别召回 6 条/2 条且文档正确 |
| P1-3 拒答过滤引用 | `ChatService` 新增 `isNoContentAnswer` 判定(跳过开头 Markdown 装饰匹配标志语):同步/流式拒答时落库与返回空引用,SSE `done` 事件带 `refused` 标记,前端收到后撤下已推送 refs;系统提示词补充"拒答时不标注引用编号" | 越界题 references 5→**0**,回答为干净的一句"知识库中暂无相关内容。" |
| P1-4 重排实际状态 | `/api/admin/kb/status` 新增 `rerankEnabled`/`rerankActive` 分离;管理端开关旁加状态标签 | 当前环境返回 `rerankEnabled=true, rerankActive=false`,页面显示"已开启但未生效" |

**复测结果(同一 8 题基准)**:

| 指标 | 优化前(09-11) | 优化后(09-12) |
|------|--------------|--------------|
| 中文在库题命中 | **2/5** | **5/5** ✅ |
| 英文在库题命中 | 2/2 | 2/2 |
| 越界题 references | 5 条噪声 | **0 条** ✅ |
| 有效回答延迟 | 2.6–3.7 s | 2.3–3.1 s |
| 空召回拒答延迟 | 0.6–2.1 s | 1.2 s(拒答仍需一次 LLM 调用) |

结论:两条 P0 互补——含英文术语的中文 query 由关键词路兜住,纯中文 query 由降阈值兜住;P1 把降阈值引入的越界噪声在引用展示层收口。跨语言分数压低是嵌入模型固有限制,若后续外文教材占比继续升高,再评估 P3 换 bge-m3。
