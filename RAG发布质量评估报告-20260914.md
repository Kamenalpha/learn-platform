# RAG 项目发布质量评估报告

- **评估日期**：2026-09-14
- **评估对象**：E:\RAG 通用多学科智能学习平台（Spring Boot 3.5 + Spring AI 1.1.8 + Vue3 + MySQL/Chroma/Redis）
- **评估基准**（已与项目负责人确认）：
  - 目标场景：毕业答辩演示 + 写入简历的项目经历
  - 评估范围：全栈全模块（后端 93 个 Java 文件、前端 35 个源文件、SQL/部署配置/测试）
  - 阻断级标准：可运行性、安全底线、测试与回归、代码规范一致性
  - docker-compose 链路纳入发布前必测清单（当前未实测）
- **评估方式**：静态代码审查（5 个并行专项：后端安全、后端质量、RAG 链路一致性、前端、部署与数据/测试）+ 实际执行编译/构建/测试验证

---

## 一、总体结论

**当前不满足发布条件。** 存在 6 项阻断级问题（2 项安全、3 项可运行性/文档失实、1 项未实测项），修复后预计可达发布门槛。

整体工程质量高于一般毕设水平：后端 34 个测试全部通过、越权防护在绝大多数模块到位、无 TODO/System.out/死代码注释残留、IO 全部 try-with-resources、公开内容审核防自批准逻辑正确。但下列问题真实存在且可复现，均附代码证据。

| 阻断标准 | 判定 | 依据 |
|---|---|---|
| 可运行性 | ✗ | 编译/构建/测试全过，但按 README 走的手动部署路径必坏（P0-3）、静默残废（P0-4），Docker 路径配置不全（P0-5）且未实测（P0-6） |
| 安全底线 | ✗ | JWT 密钥硬编码可伪造管理员（P0-1）、两处越权读他人数据（P0-2） |
| 测试与回归 | △ | 后端 34 例全过（覆盖鉴权/越权/考试/配额/RAG 六类），前端 6 例全过；但审计模块零测试（待确认项 D-5） |
| 代码规范一致性 | ○ | 符合 PROJECT_CONVENTIONS，分层目录/提交规范/SQL 归置均达标；存在 controller 层业务下沉不足、N+1 查询等建议级问题 |

---

## 二、阻断级问题（不修不可发布）

### P0-1｜JWT 签名密钥硬编码且无环境变量覆盖入口

- **位置**：`backend/src/main/resources/application.yml:73`
- **证据**：`jwt-secret: rag-edu-graduation-project-jwt-secret-please-change-in-prod` 为纯字面量，无 `${JWT_SECRET:...}` 占位（同文件 LLM_API_KEY 等均有占位）；`docker-compose.yml:68-84` 的 environment 块亦未注入该变量。`common/JwtUtil.java:23` 直接用它生成 HS256 密钥。
- **影响**：项目公开到 GitHub 后，任何读者可签发 `role=1` token 直接通过 `AuthInterceptor.java:43` 的管理员校验，获得全部管理端权限（删用户、改角色、审核、重建知识库）。
- **建议**：改为 `jwt-secret: ${JWT_SECRET:<默认值>}`，并同步补入 README 环境变量表、`.env.example`、docker-compose.yml。

### P0-2｜两处越权（IDOR）：任意登录用户可读取他人项目辅导详情与学习计划详情

- **位置**：
  - `service/ProjectService.java:74-80`：`detail(projectId)` 未传 userId，无归属比对（同 Controller 的 delete 传了 userId，唯独 detail 漏了）
  - `service/StudyPlanService.java:31-39`：`detail(planId)` 同样问题，返回计划含全量任务
- **影响**：遍历 ID 即可读取任意用户的课设题目、AI 生成的完整报告（含 techSolution/docContent 大段文本）、学习计划与任务。
- **佐证**：知识库 [[RAG项目-数据隔离与权限边界]] 沉淀过统一授权模型，docs/exam/chat/community 等模块归属校验经逐项抽查全部到位，唯这两处漏网，判定为遗漏而非设计（详见待确认项 D-1）。
- **建议**：补归属校验（比对 `UserContext.getUserId()`），并仿照 `CourseAccessServiceTest` 各新增一条越权测试用例。

### P0-3｜README 手动部署步骤照做必坏：资讯抓取 INSERT 报错

- **位置**：`sql/init_learning.sql:500-514`（knowledge_news 建表无 `content` 列）vs `sql/upgrade_news_fulltext.sql:12`（content 列仅由该脚本添加）；`README.md:87-90` 手动部署仅要求执行 `init_learning.sql` 与 `upgrade_guest_public_news.sql`。
- **影响**：实体 `KnowledgeNews.java` 含 content 字段，`NewsService.java:152-159` 抓取入库时 MyBatis-Plus INSERT 直接报 `Unknown column 'content'`。评委按 README 复现时每日资讯功能当场报错。Docker 路径因 sql 目录按字母序自动执行全部脚本而幸免。
- **建议**：README 步骤 1 改为依次执行 `init_learning.sql → upgrade_fulltext_index.sql → upgrade_news_fulltext.sql`；或直接把 content 列与全文索引并入 init_learning.sql（更彻底）。

### P0-4｜README 遗漏 upgrade_fulltext_index.sql → 混合检索静默降级为仅向量

- **位置**：README 第六节（148 行）写明该脚本必执行，但快速开始（86-90 行）未包含。
- **影响**：降级路径真实存在（`RetrievalServiceTest.keywordIndexMissingDegradesToVectorOnly` 测试印证）且无任何报错。按 README 手动建库后，「混合检索 + RRF 融合」实际只剩单路向量召回，检索测试页看不到关键词路数据，与论文/README 承诺不符。
- **建议**：同 P0-3，并入 init_learning.sql 或写进快速开始。

### P0-5｜compose 未给 backend 传入 QUOTA_\*/NEWS_\* 环境变量，.env 模板形同虚设

- **位置**：`docker-compose.yml:68-84` 的 environment 块仅有 DB/REDIS/CHROMA/LLM/EMBED/RERANK_API_KEY/OCR_BASE_URL；`.env.example:22-27` 的 QUOTA_* 与 README:144-145 的 `NEWS_FETCH_CRON`/`NEWS_RSS_FEEDS` 均未被引用。
- **影响**：compose 中 `.env` 只做变量替换，未在 `environment:` 引用的变量不进容器——Docker 部署下配额开关、资讯源覆盖、抓取 cron 全部静默失效，与 README 承诺不符。
- **建议**：environment 块补齐 `QUOTA_ENABLED`、五个 `QUOTA_*_LIMIT`、`NEWS_FETCH_CRON`、`NEWS_RSS_FEEDS`（RSS 列表含逗号，需整段引用）。

### P0-6｜docker-compose 一键部署链路从未实测（已指定发布前补实测）

- **位置**：`docs/实现进展.md:57` 自述「未实测」，而 `README.md:60` 宣传「推荐体验，约 5 分钟」。
- **静态核查结论（好消息）**：sql 挂载目录按字母序执行为 `init.sql → init_learning.sql → upgrade_ai_diagnosis → upgrade_fulltext_index → upgrade_guest_public_news → upgrade_news_fulltext`（`.` ASCII 46 < `_` 95），所有 upgrade 自带 `USE learn_platform` 且依赖表均已建，**不存在建库顺序/错库问题**。
- **遗留风险**：`docker-compose.yml:46-55` chroma 服务无 healthcheck，backend 仅 `service_started` 即启动，存在启动竞态（Spring AI 启动时建 Collection），当前靠 `restart: unless-stopped` 兜底反复重启。
- **建议**：发布前完整跑一次 `docker compose up -d --build` 并走完 README 七步验收；给 chroma 加 healthcheck（`wget -qO- http://localhost:8000/api/v2/heartbeat`）并将 backend 改为 `service_healthy`。

---

## 三、高风险问题（强烈建议发布前处理）

| # | 问题 | 位置 | 摘要与建议 |
|---|------|------|-----------|
| H1 | 管理员校验依赖 URI 前缀匹配 | `AuthInterceptor.java:43` | `getRequestURI().startsWith("/api/admin")` 在非根 context-path 部署时恒为 false，任意学生可调管理端接口。当前根路径部署未触发，属隐性炸弹。建议改用 handler mapping/path pattern 或注册管理拦截器 |
| H2 | 事务内调 LLM（长事务） | `ExamPracticeService.java:76-106/200-278/427-462` | 出题/交卷判分/变式生成在 `@Transactional` 内串行调 LLM（读超时 5 分钟），全程占用 DB 连接，并发交卷易耗尽连接池；事务内还嵌套 Redis 调用。建议 LLM 调用移出事务，拆「生成」与「持久化」两段 |
| H3 | 错误响应泄露内部异常 | `ChatService.java:96`、`ExamPracticeService.java:110/514`、`IngestService.java:109`、`TextExtractor.java:59`、`DocumentService.java:60`、`GraphService.java:103`、`ChatService.java:152`(SSE) | `e.getMessage()` 拼入 BizException 透传前端，可泄露服务器路径/上游 API 报错。建议对外统一通用文案，detail 记日志 |
| H4 | 悬浮球拖拽监听器移除时事件名写错 | `frontend/src/components/FloatingChat.vue:95-101` | 注册 `mousemove/mouseup` 但移除 `pointermove/pointerup`，监听器永不清理，多次拖拽后位置跳变、内存持续增长。两行修复（参照同文件 startDlgDrag 写法） |
| H5 | 多表删除缺 @Transactional | `CommunityService.java:142-145`（删帖四表）、`StudyPlanService.java:63-64`、`AssistantService.java:98-100`、`CommunityService.java:87-92` | 中途失败留孤儿数据。统一补 `@Transactional(rollbackFor = Exception.class)` |
| H6 | 默认弱口令 + 登录无限速 | `DataInitializer.java:28-41`（admin/admin123、student/123456，明文打进启动日志）、`AuthController.java:24-33`（无失败锁定） | 演示场景可接受，对外部署须加 `INIT_DEFAULT_ACCOUNTS=false` 开关并移除日志中的明文口令 |
| H7 | Redis 单点放大 | `KbConfigService.java:33-53` | 无 try/catch，Redis 宕机 → 问答/出题/检索全部 500（对比 QuotaService/ChatService 历史均有降级）。建议 try/catch 回落 RagProperties 默认值 |
| H8 | 三个死代码视图约 420 行 | `frontend/src/views/Documents.vue`、`Courses.vue`、`ExamPoints.vue` | 未挂路由却被构建进产物；Documents.vue 与 KnowledgeBase.vue 大段重复。建议删除 |
| H9 | README 端口写错 | `README.md:71/119` 写 5173，`frontend/vite.config.js:8` 实际 5174 | 答辩演示按 README 打开即失败，一行修复 |
| H10 | 生产后端地址不可配置 | `frontend/src/utils/request.js:6`、`api/index.js:136`、`vite.config.js:10-14` | baseURL 硬编码 `/api`，前后端跨域部署无切换手段。建议 `import.meta.env.VITE_API_BASE \|\| '/api'` |

---

## 四、中风险问题（建议项，答辩前后择机处理）

1. **分层违规**：`PublicController.java:43-48`（注入 5 个 Mapper + 查询拼装）、`AdminAuditController.java:39-42`（审核决定逻辑直接写库且无事务）、`ChatController.java:60-103`、`KbController.java:59-73` —— 业务应下沉 Service。
2. **并发竞态**：`CommunityService.toggleLike`（117-131）先查后插可重复点赞；`ExamPracticeService.upsertMistake`（364-379）可重复错题行；点赞/评论计数读改写丢更新。建议唯一索引 + SQL 原子自增。
3. **上传解析同步阻塞**：`DocumentService.java:76-80` 在请求线程同步解析，OCR 最长约 30 页 × 60s；纯文本层 PDF 无页数上限（`TextExtractor.java:63-83`），POI 未防 ZIP 炸弹。parse_status 状态机天然支持异步化。
4. **N+1 查询**：DocumentService.list、ExamPracticeService（多处，findLastWrongAnswer 最多 50 次循环查询）、StatsService.aiUsage、NewsService 去重。毕设数据量可跑，发布规模会放大。
5. **KbController.rebuild 吞异常**（`KbController.java:67-68`）：失败原因完全丢失，至少补 log.warn。
6. **阈值双口径**：`RagProperties.java:29` 与 `RetrievalService.java:66/137` 的代码默认 0.5，与 README/yml 的 0.4 不一致（当前被 yml 覆盖掩盖，删掉 yml 项即回 0.5）。
7. **出题模拟未复用混合检索**：`ExamPracticeService.java:548-566` 直接读 doc_chunk 前 100 块；README:150「该链路也支撑出题模拟」表述过强，真正走检索链路的是考点生成接口（`ExamService.generate`）。论文/README 建议改为「复用摄入链路的分块产物」。
8. **流式拒答仍先推送 refs**：`ChatService.java:121` SSE 模式 refs 先于答案无条件推送，靠 `done.refused` 让前端撤下，「拒答不返回引用」在 SSE 首帧层面不严格成立。
9. **debug 与正式检索异常行为不一致**：`RetrievalService.java:148-150`（吞异常继续）vs `:78-81`（抛错），「调试所见即线上所得」在 Chroma 异常分支不成立。
10. **qa_record.assistant_id 死列**：表有列（init_learning.sql:181）但 entity 无字段，从未写入，「用了哪个助手」永久丢失。补字段或删列。
11. **参数校验缺失**：多个 Controller `@RequestBody` 无 `@Valid`（Exam/Community/StudyPlan/Course/Kb），DTO 无约束注解，与 AuthDtos 风格不一致。
12. **SQL 日志 stdout**（application.yml:56 StdOutImpl）：开发期论文截图用，发布应移除。
13. **CORS 全开 + `?token=` URL 传参**：`WebConfig.java:30-36`、`AuthInterceptor.java:31`；token 会进访问日志/浏览器历史/Referer。
14. **升级脚本不可重复执行**：`upgrade_fulltext_index.sql:6` 裸 ALTER，无 information_schema 守卫（对比 upgrade_news_fulltext.sql 有守卫写法可参照）。
15. **路由守卫 JSON.parse 无容错**：`frontend/src/router/index.js:50`，userInfo 篡改为非法 JSON 时全站导航中断。
16. **前端 401 只按 body code 判定**：`request.js:36-39` 不判 HTTP 401，接入反代/网关后登出逻辑会失效。
17. **重复代码**：RetrievalService 双路召回约 60 行重复、nicknamesOf 逐字重复两处、truncate/abbreviate 五处各写一份。

---

## 五、已检查未见问题的方面（正向结论）

1. **SQL 注入零风险**：全部 mapper（含 ngram 全文检索 `DocChunkMapper.keywordSearch`）参数化绑定，无 `${}` 用户输入拼接点。
2. **密码安全**：注册/初始化均 BCrypt，登录 `passwordEncoder.matches`，用户列表返回前置空口令字段。
3. **路径穿越无向量**：上传文件名 UUID 重命名，读取时 fileUrl 恒为 UUID 名，uploads 目录未静态暴露。
4. **JWT 解析规范**：jjwt 0.12.x `parseSignedClaims` 自动校验 exp，无 none 算法/混淆风险。
5. **白名单收敛**：仅放行 login/register/news/public 五类路径，公开接口均校验 `visibility=1 && auditStatus=1` 双条件，试读限 8 块且不含原文件下载。
6. **防自批准**：公开申请服务端强制回待审（`DocumentService.java:128-137`），审核动作仅管理员可执行，设计正确（社区帖除外，见 D-2）。
7. **RAG 数据隔离 fail-closed**：向量路 courseId metadata 过滤 + 关键词路 SQL IN + 双路召回后逐条 `canReadResource` 校验；向量检索异常直接抛错不回退全库；测试覆盖印证。
8. **交卷防作弊**：开考/交卷前不返回答案、CAS 原子状态更新阻止重复交卷、过期考试不判分，均有测试。
9. **mock 不会误发布**：`import.meta.env.MODE === 'mock'` 静态替换，生产构建不含 mock-dev.js。
10. **前端守卫双保险**：admin 路由 meta + localStorage role 前端守卫，后端 `/api/admin/**` 角色拦截兜底。
11. **无敏感数据入库**：data/（含 183M 本地模型）、tools/、.env（含真实 Key）均被 .gitignore 排除且全历史核验无泄漏。
12. **规范一致性**：零 System.out/printStackTrace/TODO/FIXME/死代码注释残留；构造器注入无循环依赖；定时任务有整体异常保护与单机防重（synchronized）。
13. **验证结果**：后端 `mvn -o compile` 通过；前端 `npm run build` 通过（20s，仅 chunk 体积警告）；后端 `mvn -o test` 34/34 全过（约 1.5 分钟）；前端 `npm test` 6/6 全过。

---

## 六、测试覆盖明细

| 测试类 | 用例数 | 覆盖内容 |
|---|---|---|
| AuthInterceptorTest | 3 | 普通用户调管理端 403、管理员放行、非管理接口放行 |
| CourseAccessServiceTest | 6 | 私有课程 owner 限定、公开双条件、管理员管理他人数据、助手 owner 限定、范围检索 fail-closed |
| ExamPracticeServiceTest | 8 | 选项 JSON、交卷前不泄答案、重复交卷原子阻止、过期不判分、变式轻量构建、拒绝他人错题、LLM 失败包装、AI 评语沉淀 |
| QuotaServiceTest | 8 | 超限拒绝、管理员绕过、0=不限、Redis 故障 fail-open、计数落库、月度键过期 |
| RetrievalServiceTest | 4 | 向量+关键词 RRF 融合、无课程范围返回空、重排失败降级、无全文索引降级仅向量 |
| ChatServiceTest | 5 | 历史按用户+会话隔离、自定义提示词不能覆盖防编造规则、上下文轮数有界、引用 PDF 跳转、mapper 恢复 chunkId |
| 前端 citations.test.mjs | 6 | 引用编号拆分：无来源/范围内/连续编号/超范围/流式半截/空串 |

**空白**：审计模块、社区、学习计划、资讯、上传解析均无测试。

---

## 七、待确认项（不擅自定性，请项目负责人裁定）

| # | 事项 | 位置 | 待确认问题 |
|---|------|------|-----------|
| D-1 | ProjectService/StudyPlanService detail 越权 | 见 P0-2 | 遗漏还是为分享功能预留？评估建议按 bug 修 |
| D-2 | 社区帖发布即 auditStatus=1 | `CommunityService.java:56`（注释自认「演示:直接通过」） | 与课程/教材「公开必须审核」不一致，是否补审核流 |
| D-3 | 登录用户可读完整公开教材 | `CourseAccessService.java:58-68` | 公开页限 8 块试读，但 `/api/docs/{id}` 登录后可读全文——「登录享完整内容」还是试读绕过口 |
| D-4 | `?token=` URL 传参 | `AuthInterceptor.java:31` | 为文件预览有意设计，代价是 token 进日志/历史，是否保留 |
| D-5 | 审计模块零测试 | `backend/src/test/` | 「测试与回归」定为阻断标准，是否要求补审计用例后才算达标 |
| D-6 | 主观题 AI 评分失败记 0 分 | `ExamPracticeService.java:358-361` | 有「待人工复核」评语但无复核入口，是否接受 |
| D-7 | 本机 .env 存真实 DeepSeek Key | `.env:2` | 已确认未入 git 非泄露；若环境/截图共享过建议轮换 |
| D-8 | HTTP 状态恒 200、错误码在 body | `Result.java` + 所有 handler | 前端契约自洽，但网关/监控无法按状态码告警，是否接受 |
| D-9 | Redis 配额 fail-open | QuotaServiceTest 印证 | Redis 宕机时配额放行是有意设计，对外部署是否需要收紧 |

---

## 八、发布前行动清单（按序执行）

1. **安全两刀**（预计半天）：修 P0-1（JWT 密钥占位符）、P0-2（两处 IDOR + 新增越权测试）。
2. **文档/配置对齐**（一两小时）：修 P0-3/P0-4（README 部署步骤或合并 SQL）、P0-5（compose 环境变量补齐）、H9（端口）。
3. **Docker 实测**：完整 `docker compose up -d --build` + README 七步验收（P0-6），顺带给 chroma 加 healthcheck。
4. **按 D-2/D-5 决策**决定是否补社区审核流与审计测试。
5. **高风险批处理**：H4/H9 分钟级修复优先；H2（事务重构）工作量大，可放答辩后。

---

## 附：评估方法说明

- 后端安全/质量/RAG 链路/前端/部署与数据共 5 个专项静态审查，累计阅读 93 个 Java 文件、35 个前端源文件、全部 SQL 与部署配置。
- 实际执行验证：`mvn -o compile`（通过）、`npm run build`（通过）、`mvn -o test`（34/34 通过）、`npm test`（6/6 通过）。
- 所有结论均关联到具体文件与行号；不确定是设计意图还是缺陷的问题一律列入第七节待确认项，未擅自定性。
